# Arguments are inputs given to the function

def greet(name):
    print("Hello", name)

# Passing Argument
greet("john")

# Here we passed "john" as an argument to the greet() function
# We can pass the different arguments in each call, making the function re-usable and dynamic

# Calling a function with a different argument

greet("echo")
